import React from 'react';
import Menu from '../../features/Menu/Menu';

const Navbar = (props) => {
    return (
        <div>
            Menu: <Menu />
        </div>
    )
}

export default Navbar;